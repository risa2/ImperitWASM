@code {
	Description? type = null;
	public void Open(Description new_type)
	{
		type = new_type;
		StateHasChanged();
	}
}

<PopupBox Show="type is Description" OnClose="() => type = null">
	@type?.Name (@type?.Symbol) &ndash; @(new MarkupString(string.Join("<br/>", type!.Text)))
</PopupBox>